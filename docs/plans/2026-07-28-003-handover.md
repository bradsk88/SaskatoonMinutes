---
title: "Handover: the site publishes two Atom feeds, an agenda item has an address, and the card ranking separates long debates again"
type: handover
status: active
date: 2026-07-28
---

# Handover

Read `CLAUDE.md` first — brevity, bullets, no jargon.

Supersedes `2026-07-28-002-handover.md`. Its biggest open item — the five
organizations that are still job titles — is **still open and untouched**;
it costs a full summarize run and remains Brad's call.

## State

- Branch **`main`**, HEAD `2fa7079`, **pushed and deployed**, tree clean.
  `origin/main` matches. Brad pushes; a push to `main` deploys.
- **828 tests pass**: `./venv/bin/python -m pytest tests/ -q` (~2.4s,
  offline). Was 771.
- Seven commits this session: `f28110e`, `d542cdc`, `0ca3b3e`,
  `32879b1`, `dc69963`, `1208172`, plus the styling pass below.
- The eval still **exits 1** on the pre-existing chip faithfulness floor.
  Untouched by this work.
- **The feeds are live and verified**, 2026-07-28:
  - `https://yxeminutes.ca/feed.xml` — 139 KB, 30 day entries, March 20
    to July 21.
  - `https://yxeminutes.ca/feed-items.xml` — 120 KB, 100 item entries.
  - Both parse as valid XML; all ids unique and under the site's own
    domain; `self` links correct; autodiscovery and the footer link both
    served on the live page.
  - Nothing is being held back by the settle rule — July 21 is genuinely
    the most recent meeting the city has held. The seven-day fallback has
    not had to fire yet, so that path is still unexercised in production.

## Do this next

Nothing is blocked. **Item 15 is done** — see below; the handover was
written before it was fixed.

1. **Point a real feed reader at it once.** Everything measurable has
   been checked; how the entries actually read in a reader has not.
2. **`TODO.md` item 13** — the broken discussion spans. Not a blocker
   any more (the >3h guard holds), but duration now carries more weight,
   so repairing the data is worth more than it was.
3. Ratify ADRs. **Ten wait on Brad**, `0018` and `0019` newest — and
   `0018` carries an amendment correcting one of its own claims.

## What landed this session

### Two Atom feeds (`TODO.md` item 12, done)

`app/feeds.py`, written on every build:

- `/feed.xml` — one entry per calendar day the city sat, bodies as
  headings inside it. The default.
- `/feed-items.xml` — one entry per qualifying item.

Both use one gate and differ only in granularity. An item qualifies with
a Description **or** an interpretive chip; qualifying items rank by
`discussion_minutes` and cap at 8 per meeting.

A day publishes once **settled** — every meeting on it has summaries, or
seven days have passed. Stateless: entry ids and `<updated>` come from
the meeting date, so the six-times-a-day deploy produces one entry.

Verified against two meetings pulled live from eSCRIBE with their cached
summaries: 2 day entries, 16 item entries, all five speakers on the
Downtown Entertainment District item named with their organizations.

### The feed does not reuse the card's ranking — ADR `0018`

This is the session's most reusable finding. `extract_meeting_topics`
looked like the right selector and is not. Measured, same substance gate
both sides, top 8 each:

| meeting | what the card's ranking misses |
|---|---|
| 2025-11-25 budget | Capital Options (155.5 min), Housing and Homelessness funding (83.6), Arts, Culture and Events Venues (81.3) |
| 2025-12-03 | Downtown Event and Entertainment District (100.3), Development Incentives Policy (71.1) |
| 2025-12-17 | 6 of 8 agree; misses one 11.0-minute item |

Instead it includes Land Development (3.5), Community Support (10.2) and
a right-of-way dedication (1.4).

Cause was one line: `0.25 * min(1, minutes / 20)`, flat above twenty
minutes, so a 155-minute debate and a 21-minute one scored the same and
the tie fell to dollar signs and section-number depth.

**Fixed later the same session** — log-scaled, weight 0.45, full at two
hours. Over 12 real meetings, recall of a meeting's longest substantive
discussions went **24/29 (82.8%) → 28/29 (96.6%)**, with only 4 of 96
card slots changing: every row in was a long debate, every row out was
under 11 minutes. `TODO.md` item 15 has the numbers.

The important part is not the fix but how it was found: **the shape was
not the problem, the weight was.** Log-scaling and raising the cap at the
old 0.25 weight both made recall *worse*. Only measuring showed that.

`extract_meeting_topics` also returns **speaker rows mixed in with
items**, so a caller cannot treat its return value as a list of agenda
items. (An earlier claim that this left the feed short of its cap was
wrong — the speaker rows are appended, not competing. ADR `0018` is
corrected.)

### An agenda item has an address (`TODO.md` item 7, done)

`#item-<item_id>` on every card, set as it is drawn, with the page
scrolling after render — a fragment resolved against a skeleton is
dropped, which is what made this look impossible.

The index card links to it too. A row's href carries both parts:
`?t=<ms>` seeks the video, `#item-<id>` places the reader. A consent item
gets the anchor and no seek, so those rows are addressable for the first
time.

Arrival scrolls instantly and twice on purpose — scrolling activates the
sticky player, which changes the height of everything above the target,
so the first jump lands short. A `?t=` click still scrolls smoothly once.

### Index card, at Brad's request

The organization chip moved from after the speaker's name to the right,
beside the stance. The stance badge is now **outlined** where an outcome
badge is filled — green for support, rose for concerns.

The outline is doing two jobs. It answers the objection recorded at
`style.css:1108` (a filled green badge in the outcome's slot read as
"council passed it"), and it stops the stance blurring into the pastel
organization chip now touching it.

**All three stance badges are one width** (`min-width: 6.5rem`, centred),
so the column's right edge is straight rather than ragged across "Spoke",
"In support" and "Raised concerns".

**A card row is clickable for its whole height.** The row's vertical
space used to sit on the table cell, outside the link, so most of a row
did nothing when clicked. It moved to `.topic-content` with a negative
horizontal margin, so the link reaches the cell's edges without the text
moving. Row height is unchanged — verified by outlining the link box and
screenshotting, since nothing else shows a hit area.

**A neutral stance renders "Spoke"**, in a dashed muted outline. It was
briefly removed — it says little on its own — and Brad put it back for
layout consistency: a row with no badge where every row above has one
reads as a rendering fault. Dashed rather than solid so it does not
become a second grey pill beside the "Resident" chip.

### Smaller

- `TODO.md` item 1 was **stale**: every title path was already escaped.
  The one real gap was the meeting id going into an `href` unencoded;
  now `encodeURIComponent` at the source, `escapeAttr` at the seam.
- The AI disclaimer no longer appears under "No AI summary available" —
  there was no AI output there to qualify.
- Feeds advertised by autodiscovery on both pages plus one footer line.
- `readable_date` has one home. `discussion_minutes`, `item_summary`,
  `CARD_CHIP_CATEGORIES` and `TAKEAWAY_ORDER` are public so the feed
  shares them. A test parses `index.html` and fails if its takeaway
  order drifts from the Python one.

## Traps

- **A browser screenshot cannot verify a JS scroll.** Two Firefox
  screenshots showed the page at the top and both were wrong — the
  capture lands before the scroll. This cost real time and nearly led to
  "fixing" working code. **Use jsdom instead**, it is now proven:
  `npm install jsdom@24` in the scratchpad, `runScripts: 'dangerously'`,
  and stub `IntersectionObserver`, `scrollTo`, `animate` and
  `getBoundingClientRect` in `beforeParse` — jsdom lays nothing out, so
  every rect is top 0 unless you fake one. The probe printed 36 items,
  36 ids, and the exact scroll calls per URL shape.
- **`jsdom@latest` does not run on this Node.** `ERR_REQUIRE_ESM` from
  `html-encoding-sniffer`. Pin `jsdom@24`.
- **`loadMeeting`'s `try/catch` swallows everything** and reports
  "Failed to load meeting details." That is how the jsdom gap presented,
  and it wasted a cycle looking for a page bug. Now `TODO.md` item 16.
- **`_site/` still predates the roster.** Unchanged from `002`. Build a
  preview by fetching live with
  `EscribeMeetingSource(LiveEscribeTransport())` and pulling summaries
  with `git show origin/summaries:summaries/<id>.json`. The two scratch
  scripts that do this are worth rewriting rather than hunting for.
- **`firefox --screenshot` still needs the absolute path first**, never
  `--headless` first, and a preview copied to another directory loses
  its relative `style.css`.
- **Cache a corpus before tuning anything.** Twelve real meetings with
  their summaries, fetched once into a JSON file, made the ranking work
  possible — every variant then cost seconds instead of a fetch. Three
  meetings had been enough to *find* the defect and were not enough to
  *fix* it: the three-meeting sample said the shape was wrong, and over
  twelve it was the weight. `fetch_corpus.py` picks the twelve largest
  files on the summaries branch, which is a good proxy for "meetings with
  enough coverage to rank".
- **Measure the fix, not only the bug.** Two of the four candidate
  formulas made recall *worse* than the defect they were meant to fix,
  and both were the ones the TODO had proposed from reading the code.
- **Do not measure a ranking by reading it.** Both real findings this
  session — the saturation and the speaker rows in the topic list — came
  from running the real function over real meetings and diffing. Neither
  was visible in the code.
- Everything in `002` still applies: the whole transcript slice matters,
  match speakers by organization, `git commit` fails on an unchanged
  re-save, `deploy.yml` fires on the summarize run **completing** rather
  than succeeding, `summarize_meetings.py` does not load `.env`, the
  judge is noisy, no `gh` and no GitHub token, model is
  `gemini-2.5-flash`.

## Still open — do not silently decide these

- **The feeds have never been served.** Everything is verified offline
  and against live eSCRIBE data, but no reader has ever fetched
  `yxeminutes.ca/feed.xml`. First deploy is the real test. Worth
  checking the file validates and that the day entries look right before
  telling anyone to subscribe.
- **Five organizations are still job titles** — `Board Chair`,
  `Property Owner`, `Commercial Property Owner`, `CEO of Nutrien
  Wonderhub`, `Director of Planning and Development`. Unchanged from
  `002`: only a re-run with `force=true` fixes them, which is a full
  summarize pass. Brad's call.
- **Feed decisions deliberately not made**: no per-body feeds (sixteen
  bodies, fifteen would be empty for months — the body rides as an Atom
  `<category>`); retention is 30 days and 100 items, not the archive; no
  header link, footer and autodiscovery only.
- **The two pages still count differently.** Index card says "N other
  items"; detail header says "19 discussed · 17 approved without debate".
  Unchanged from `002`, still undecided.
- **Zoning codes decoded from memory.** Unchanged and unanswered across
  six handovers. It is the fix for the red eval. Recommended: give the
  prompt Saskatoon's zoning table.
- **One staff leak survives, on purpose** — "Tony Landry presented the
  report." Unchanged from `002`.
- **A speaker has no play button and no anchor of their own.** Their card
  row now lands on the item they spoke to, which is closer than before
  but still not the moment they started talking. Needs a per-speaker
  timestamp: have the speaker pass return a short verbatim phrase from
  each opening, then locate it in the transcript segments. Untried.
- **Ten ADRs wait on Brad**, `0018` and `0019` newest.
- **A defeated item should say why** — `TODO.md` item 14, unstarted.
